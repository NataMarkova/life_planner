<!DOCTYPE html>
<html lang="en">
  <head>
    <title>New goal Activity</title>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-beta/css/bootstrap.min.css" integrity="sha384-/Y6pD6FV/Vv2HJnA6t+vslU6fwYXjCFtcEpHbNJ0lyAFsXTsjBbfaDjzALeQsN6M" crossorigin="anonymous">
    <link rel="stylesheet" href="/Users/nataliamarkova/Code_The_Dream/lessons/goal-planner/app/views/lives/css/custom.css">
  </head>
  <body id="home" data-spy="scroll" data-target=".navbar" data-offset="100">
  
    <!-- Page Content -->
    
    <div class="container">
      <div class="page">
        <div class="page-content">
          <%= form_for(goal) do |f| %>
            <% if goal.errors.any? %>
              <div id="error_explanation">
                <h2><%= pluralize(goal.errors.count, "error") %> prohibited this goal from being saved:</h2>
                <ul>
                <% goal.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
                </ul>
              </div>
            <% end %>

            <div class="col-sm-8 form-control">
              <div class="item-title item-label"> 
              <b><p>Set a goal that you want to achieve</p></b>
              </div>
              <div class="item-title item-label">
                  <%= f.text_field :create_goal %>
                  <p class="small">Make sure your goal is clear and reachable</p>
              </div>
            </div>

            <div class="col-sm-8 form-control">
              <div class="item-title item-label"> 
                <b><p>Choose task or activity that you created previously</p></b>
              </div>

              <div class="field">
                <%= f.collection_select :life_id, Life.all, :id, :life_name, include_blank: false %>
              </div>
            </div>

            <div class="col-sm-8 form-control">
              <div class="item-title item-label"> 
                <b><%= f.label :start_date %></b>
              </div>
              <div class="item-title item-label">
                <%= f.date_select :start_date, start_year: 2000, end_year: 2050 %>
                <p class="small">Define the date when you want to start working on the goal</p>
              </div>
            </div>

            <div class="col-sm-8 form-control">
              <div class="item-title item-label"> 
                <b><%= f.label :end_date %></b>
              </div>
              <div>
                <%= f.date_select :end_date, start_year: 2015, end_year: 2030%>
                <p class="small">Define the date when you want to reach the goal</p>
              </div>
            </div>

            <div class="col-sm-8 form-control">
              <div class="item-title item-label"> 
                <p>
                  <b><p>Has the goal already achieved?</p></b><br />
                  <%= f.radio_button :achieved, 'Yes'%>Yes<br />
                  <%= f.radio_button :achieved, 'Not yet'%>Not yet<br />
                </p>
              </div>
            </div>

            <div class="col-sm-8 form-control">
              <div class="item-title item-label"> 
                  <b><p>Notes & Ideas</p></b>
              </div>
              <div class="item-title item-label">
                  <%= f.text_field :note_goal %>
                  <p class="small">Leave notes for a goal you want to reach</p>
              </div>
            </div>
            
            <div class="form-actions">
              <button type="submit" class="btn btn-primary">Create</button>
              <button type="button" class="btn"><%= link_to 'Cancel', goals_path %></button>
            </div>

          <% end %>
        </div>
      </div>
    </div>
  </body>
</html>

  
